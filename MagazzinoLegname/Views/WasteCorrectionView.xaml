<UserControl x:Class="MagazzinoLegname.Views.WasteCorrectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="WasteLeftHeaderStyle" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="WasteCenteredHeaderStyle" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="WasteLeftCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="WasteCenteredCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="WastePanelValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24,8,24,24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="245"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border Background="#172839" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="13,9" Margin="0,0,12,0"><TextBlock Text="{Binding PendingCountText}" FontWeight="Bold"/></Border>
                <StackPanel Width="220"><TextBlock Text="OPERATORE RETTIFICA" Style="{StaticResource FieldLabelStyle}"/><ComboBox ItemsSource="{Binding Operators}" SelectedItem="{Binding SelectedOperator}"/></StackPanel>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,0,14" ClipToBounds="True">
            <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                <StackPanel Margin="16,12"><TextBlock Text="GRUPPI CLASSIFICATI · SCARTI DA VERIFICARE" FontWeight="Bold"/><TextBlock Text="Sono mostrati solamente i gruppi classificati non ancora disponibili." Style="{StaticResource PageSubtitleStyle}"/></StackPanel>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionUnit="FullRow" RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Carico" Width="80" Binding="{Binding LoadNumber, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Fornitore" Width="260" MinWidth="165" Binding="{Binding SupplierName, Mode=OneWay}" HeaderStyle="{StaticResource WasteLeftHeaderStyle}" ElementStyle="{StaticResource WasteLeftCellStyle}"/>
                        <DataGridTextColumn Header="Data" Width="105" Binding="{Binding ArrivalDate, Mode=OneWay, StringFormat=dd/MM/yyyy}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Sp. ingresso" Width="100" Binding="{Binding Group.IncomingThickness, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Sp. conv." Width="90" Binding="{Binding Group.ConventionalThickness, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Larg. ingresso" Width="110" Binding="{Binding Group.IncomingWidth, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Dopo prepiall." Width="110" Binding="{Binding Group.WidthAfterPlaning, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Lunghezza" Width="100" Binding="{Binding Group.IncomingLength, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Qualità" Width="75" Binding="{Binding Group.Quality, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Pacchi" Width="70" Binding="{Binding Group.PackageCount, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Pezzi" Width="80" Binding="{Binding Group.InitialPieces, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="MC utili teorici" Width="125" Binding="{Binding Group.TheoreticalUsefulCubicMeters, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="18">
            <Grid>
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                <Grid Margin="0,0,0,16">
                    <StackPanel><TextBlock FontSize="19" FontWeight="Bold"><Run Text="Rettifica gruppo · carico "/><Run Text="{Binding SelectedGroup.LoadNumber, Mode=OneWay}"/></TextBlock><TextBlock Foreground="{DynamicResource SecondaryTextBrush}"><Run Text="{Binding SelectedGroup.Group.IncomingThickness, Mode=OneWay, StringFormat={}{0:N2}}"/><Run Text=" × "/><Run Text="{Binding SelectedGroup.Group.WidthAfterPlaning, Mode=OneWay, StringFormat={}{0:N2}}"/><Run Text=" × "/><Run Text="{Binding SelectedGroup.Group.IncomingLength, Mode=OneWay, StringFormat={}{0:N2}}"/><Run Text=" · Qualità "/><Run Text="{Binding SelectedGroup.Group.Quality, Mode=OneWay}"/></TextBlock></StackPanel>
                    <Border HorizontalAlignment="Right" Background="#26351F" BorderBrush="{DynamicResource SuccessBrush}" BorderThickness="1" CornerRadius="9" Padding="12,7"><TextBlock Text="Classificato · scarti da verificare" Foreground="{DynamicResource SuccessBrush}" FontWeight="Bold"/></Border>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="18"/><ColumnDefinition/><ColumnDefinition Width="18"/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <Border Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="11" Padding="17">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="18"/><RowDefinition Height="Auto"/><RowDefinition Height="22"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <TextBlock Text="DATI INIZIALI" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            <StackPanel Grid.Row="2"><TextBlock Text="PEZZI INIZIALI" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.Group.InitialPieces}" Style="{StaticResource WastePanelValueStyle}"/></StackPanel>
                            <StackPanel Grid.Row="4"><TextBlock Text="MC UTILI TEORICI" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.Group.TheoreticalUsefulCubicMeters, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}"/></StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="11" Padding="17">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <TextBlock Text="SCARTO TAVOLE INTERE" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            <StackPanel Grid.Row="2"><TextBlock Text="TAVOLE INTERE SCARTATE" Style="{StaticResource FieldLabelStyle}"/><TextBox Style="{StaticResource NumericTextBoxStyle}" Text="{Binding SelectedGroup.DiscardedWholeBoards, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/></StackPanel>
                            <Grid Grid.Row="4"><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="16"/><ColumnDefinition/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="PEZZI BUONI" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.GoodPieces}" Style="{StaticResource WastePanelValueStyle}"/></StackPanel><StackPanel Grid.Column="2"><TextBlock Text="SCARTO TAVOLE %" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.WholeBoardWastePercentage, StringFormat={}{0:N2}%}" Style="{StaticResource WastePanelValueStyle}"/></StackPanel></Grid>
                            <StackPanel Grid.Row="6"><TextBlock Text="MC DOPO SCARTO TAVOLE" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.CubicMetersAfterWholeBoardWaste, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}"/></StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="4" Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="11" Padding="17">
                        <Grid>
                            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/><RowDefinition Height="14"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                            <TextBlock Text="SCARTO PARZIALE E RISULTATO" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            <StackPanel Grid.Row="2"><TextBlock Text="SCARTO PARZIALE %" Style="{StaticResource FieldLabelStyle}"/><TextBox Style="{StaticResource NumericTextBoxStyle}" Text="{Binding SelectedGroup.PartialWastePercentage, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/></StackPanel>
                            <StackPanel Grid.Row="4"><TextBlock Text="MC SCARTO PARZIALE" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.PartialWasteCubicMeters, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}"/></StackPanel>
                            <StackPanel Grid.Row="6"><TextBlock Text="MC REALI DISPONIBILI" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedGroup.RealAvailableCubicMeters, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}" Foreground="{DynamicResource AccentBrush}"/><TextBlock Text="{Binding SelectedGroup.TotalClassificationWastePercentage, StringFormat=Scarto totale classificazione: {0:N2}%}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,5,0,0"/></StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,16,0,0">
                    <TextBlock Text="Il risultato viene salvato come snapshot storico del gruppo. I singoli pacchi non vengono modificati." Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                    <Button Content="Conferma rettifica e rendi disponibile" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Click="Confirm_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
