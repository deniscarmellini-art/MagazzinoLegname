<UserControl x:Class="MagazzinoLegname.Views.WasteCorrectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style x:Key="WasteHeaderStyle" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
            <Setter Property="Background" Value="{DynamicResource SurfaceRaisedBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WasteLeftHeaderStyle" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource WasteHeaderStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
        </Style>
        <Style x:Key="WasteCenteredHeaderStyle" TargetType="DataGridColumnHeader"
               BasedOn="{StaticResource WasteHeaderStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="WasteLeftCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="WasteCenteredCellStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="WastePanelValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="ExtraBold"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style x:Key="WasteSectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        </Style>
        <Style x:Key="WasteTableTitleStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceHeaderBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="CornerRadius" Value="13,13,0,0"/>
            <Setter Property="Padding" Value="16,12"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24,8,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="55*"/>
            <RowDefinition Height="45*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,0,14" ClipToBounds="True">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border Style="{StaticResource WasteTableTitleStyle}">
                    <StackPanel>
                        <TextBlock Text="GRUPPI CLASSIFICATI · SCARTI DA VERIFICARE" Style="{StaticResource WasteSectionTitleStyle}"/>
                        <TextBlock Text="Sono mostrati solamente i gruppi classificati non ancora disponibili." Style="{StaticResource PageSubtitleStyle}"/>
                    </StackPanel>
                </Border>
                <DataGrid Grid.Row="1" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" SelectionUnit="FullRow" RowHeight="40">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource WasteHeaderStyle}"/>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Carico" Width="145" Binding="{Binding LoadNumber, Mode=OneWay}" HeaderStyle="{StaticResource WasteLeftHeaderStyle}" ElementStyle="{StaticResource WasteLeftCellStyle}"/>
                        <DataGridTextColumn Header="Fornitore" Width="195" MinWidth="165" Binding="{Binding SupplierName, Mode=OneWay}" HeaderStyle="{StaticResource WasteLeftHeaderStyle}" ElementStyle="{StaticResource WasteLeftCellStyle}"/>
                        <DataGridTextColumn Header="Data" Width="100" Binding="{Binding ArrivalDate, Mode=OneWay, StringFormat=dd/MM/yyyy}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Sp. ingresso" Width="95" Binding="{Binding Group.IncomingThickness, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Sp. conv." Width="85" Binding="{Binding Group.ConventionalThickness, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Larg. ingresso" Width="105" Binding="{Binding Group.IncomingWidth, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Dopo prepiall." Width="105" Binding="{Binding Group.WidthAfterPlaning, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Lunghezza" Width="95" Binding="{Binding Group.IncomingLength, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Qualità" Width="70" Binding="{Binding Group.Quality, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Pacchi" Width="60" Binding="{Binding Group.PackageCount, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="Pezzi" Width="60" Binding="{Binding InitialPieces, Mode=OneWay}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTextColumn Header="MC base rettifica" Width="145" Binding="{Binding AdjustmentBaseCubicMeters, Mode=OneWay, StringFormat=N2}" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}" ElementStyle="{StaticResource WasteCenteredCellStyle}"/>
                        <DataGridTemplateColumn Header="Operatore" Width="190" HeaderStyle="{StaticResource WasteCenteredHeaderStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{Binding DataContext.Operators, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                              SelectedItem="{Binding SelectedOperator, UpdateSourceTrigger=PropertyChanged}"
                                              Height="30" Margin="6,3" Padding="8,2"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="18">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Style="{StaticResource WasteSectionTitleStyle}">
                            <Run Text="Rettifica gruppo · carico "/>
                            <Run Text="{Binding SelectedGroup.LoadNumber, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock Foreground="{DynamicResource SecondaryTextBrush}">
                            <Run Text="{Binding SelectedGroup.Group.IncomingThickness, Mode=OneWay, StringFormat={}{0:N2}}"/>
                            <Run Text=" × "/>
                            <Run Text="{Binding SelectedGroup.Group.WidthAfterPlaning, Mode=OneWay, StringFormat={}{0:N2}}"/>
                            <Run Text=" × "/>
                            <Run Text="{Binding SelectedGroup.Group.IncomingLength, Mode=OneWay, StringFormat={}{0:N2}}"/>
                            <Run Text=" · Qualità "/>
                            <Run Text="{Binding SelectedGroup.Group.Quality, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="18"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Border Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="11" Padding="17">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="18"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="22"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="DATI INIZIALI" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            <StackPanel Grid.Row="2">
                                <TextBlock Text="PEZZI INIZIALI" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBlock Text="{Binding SelectedGroup.InitialPieces}" Style="{StaticResource WastePanelValueStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="4">
                                <TextBlock Text="MC BASE RETTIFICA" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBlock Text="{Binding SelectedGroup.AdjustmentBaseCubicMeters, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2" Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="11" Padding="17">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="14"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="14"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="14"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="SCARTO TAVOLE INTERE" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            <StackPanel Grid.Row="2">
                                <TextBlock Text="TAVOLE INTERE SCARTATE" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Style="{StaticResource NumericTextBoxStyle}" Text="{Binding SelectedGroup.DiscardedWholeBoards, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <Grid Grid.Row="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="PEZZI BUONI" Style="{StaticResource FieldLabelStyle}"/>
                                    <TextBlock Text="{Binding SelectedGroup.GoodPieces}" Style="{StaticResource WastePanelValueStyle}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="SCARTO TAVOLE %" Style="{StaticResource FieldLabelStyle}"/>
                                    <TextBlock Text="{Binding SelectedGroup.WholeBoardWastePercentage, StringFormat={}{0:N2}%}" Style="{StaticResource WastePanelValueStyle}"/>
                                </StackPanel>
                            </Grid>
                            <StackPanel Grid.Row="6">
                                <TextBlock Text="MC DOPO SCARTO TAVOLE" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBlock Text="{Binding SelectedGroup.CubicMetersAfterWholeBoardWaste, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <Border Grid.Column="4" Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="11" Padding="17">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="14"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="14"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="14"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="SCARTO PARZIALE E RISULTATO" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold"/>
                            <StackPanel Grid.Row="2">
                                <TextBlock Text="SCARTO PARZIALE %" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Style="{StaticResource NumericTextBoxStyle}" Text="{Binding SelectedGroup.PartialWastePercentage, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="4">
                                <TextBlock Text="MC SCARTO PARZIALE" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBlock Text="{Binding SelectedGroup.PartialWasteCubicMeters, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}"/>
                            </StackPanel>
                            <StackPanel Grid.Row="6">
                                <TextBlock Text="MC REALI DISPONIBILI" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBlock Text="{Binding SelectedGroup.RealAvailableCubicMeters, StringFormat=N2}" Style="{StaticResource WastePanelValueStyle}" Foreground="{DynamicResource AccentBrush}"/>
                                <TextBlock Text="{Binding SelectedGroup.TotalClassificationWastePercentage, StringFormat=Scarto totale classificazione: {0:N2}%}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,16,0,0">
                    <TextBlock Text="Il risultato viene salvato come snapshot storico del gruppo. I singoli pacchi non vengono modificati." Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                    <Button Content="Conferma rettifica e rendi disponibile" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Right" Click="Confirm_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
