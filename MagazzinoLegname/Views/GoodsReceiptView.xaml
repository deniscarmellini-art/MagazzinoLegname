<UserControl x:Class="MagazzinoLegname.Views.GoodsReceiptView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:MagazzinoLegname.Models"
             xmlns:behaviors="clr-namespace:MagazzinoLegname.Behaviors"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="1300">
    <UserControl.Resources>
        <Style x:Key="GridDisplayTextStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="GridInputStyle" TargetType="TextBox">
            <Setter Property="behaviors:NumericTextBoxBehavior.SelectAllOnFocus" Value="True"/>
            <Setter Property="Background" Value="#172432"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="CaretBrush" Value="{DynamicResource PrimaryTextBrush}"/>
            <Setter Property="SelectionBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="SelectionOpacity" Value="0.45"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Margin" Value="5,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="EditorBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="EditorBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="EditorBorder" Property="Background" Value="#1A2A3A"/>
                            </Trigger>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter TargetName="EditorBorder" Property="BorderBrush" Value="{DynamicResource DangerBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="0,0,0,4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="36" Height="36" CornerRadius="10" Background="{DynamicResource AccentSoftBrush}" Margin="0,0,11,0">
                            <TextBlock Text="&#xE8A5;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentBrush}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="Testata carico" FontSize="17" FontWeight="Bold"/>
                            <TextBlock Text="Dati identificativi dell'ingresso" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11"/>
                        </StackPanel>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.05*"/><ColumnDefinition Width="1.55*"/>
                            <ColumnDefinition Width=".8*"/><ColumnDefinition Width="1.1*"/>
                            <ColumnDefinition Width="1.25*"/><ColumnDefinition Width=".75*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Margin="0,0,12,0">
                            <TextBlock Text="DATA ENTRATA" Style="{StaticResource FieldLabelStyle}"/>
                            <DatePicker SelectedDate="{Binding EntryDate, UpdateSourceTrigger=PropertyChanged}"
                                        Language="it-IT" SelectedDateFormat="Short"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                            <TextBlock Text="FORNITORE" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}"
                                      DisplayMemberPath="Name"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Margin="0,0,12,0">
                            <TextBlock Text="NUMERO CARICO" Style="{StaticResource FieldLabelStyle}"/>
                            <Border Style="{StaticResource HeaderFieldBorderStyle}">
                                <TextBlock Text="{Binding DisplayLoadNumber, Mode=OneWay}" Foreground="{DynamicResource AccentBrush}" FontWeight="Bold" Margin="12,0" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="3" Margin="0,0,12,0">
                            <TextBlock Text="NUMERO DDT" Style="{StaticResource FieldLabelStyle}"/>
                            <Border Style="{StaticResource HeaderFieldBorderStyle}">
                                <TextBox Style="{StaticResource HeaderFieldTextBoxStyle}" Text="{Binding DeliveryNoteNumber, UpdateSourceTrigger=PropertyChanged}"/>
                            </Border>
                        </StackPanel>
                        <StackPanel Grid.Column="4" Margin="0,0,12,0">
                            <TextBlock Text="OPERATORE" Style="{StaticResource FieldLabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Operators}" SelectedItem="{Binding SelectedOperator}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="5">
                            <TextBlock Text="PACCHI PREVISTI" Style="{StaticResource FieldLabelStyle}"/>
                            <Border Style="{StaticResource HeaderFieldBorderStyle}">
                                <TextBox Style="{StaticResource NumericHeaderFieldTextBoxStyle}" Text="{Binding ExpectedPackages, UpdateSourceTrigger=PropertyChanged}"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                    <Border Background="#101820" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                            CornerRadius="9" Padding="11,8" Margin="0,14,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentBrush}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding SupplierProcessingLabel}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <Grid Margin="18,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.88*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="0.12*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Border Width="36" Height="36" CornerRadius="10" Background="{DynamicResource AccentSoftBrush}" Margin="0,0,11,0">
                                <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentBrush}" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel>
                                <TextBlock Text="Materiali del carico" FontSize="17" FontWeight="Bold"/>
                                <TextBlock Text="Inserisci una riga per ogni tipologia ricevuta; i campi blu sono modificabili." Foreground="{DynamicResource SecondaryTextBrush}" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Border Background="{DynamicResource SurfaceRaisedBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                                    CornerRadius="9" Padding="10,7" Margin="0,0,12,0">
                                <TextBlock FontSize="11.5" VerticalAlignment="Center">
                                    <Run Text="Certificazione: " Foreground="{DynamicResource SecondaryTextBrush}"/>
                                    <Run Text="{Binding CertificationIndicator, Mode=OneWay}" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold"/>
                                </TextBlock>
                            </Border>
                            <Border CornerRadius="9" BorderThickness="1" Padding="11,7" Margin="0,0,10,0">
                                <Border.Style><Style TargetType="Border"><Setter Property="Background" Value="#3A3020"/><Setter Property="BorderBrush" Value="{DynamicResource WarningBrush}"/><Style.Triggers><DataTrigger Binding="{Binding AreExpectedPackagesComplete}" Value="True"><Setter Property="Background" Value="#253B2B"/><Setter Property="BorderBrush" Value="{DynamicResource SuccessBrush}"/></DataTrigger><DataTrigger Binding="{Binding ArePackagesExceeding}" Value="True"><Setter Property="Background" Value="#3B2830"/><Setter Property="BorderBrush" Value="{DynamicResource DangerBrush}"/></DataTrigger></Style.Triggers></Style></Border.Style>
                                <StackPanel><TextBlock Text="{Binding PackageControlText}" FontWeight="Bold" FontSize="12"/><TextBlock Text="{Binding PackageControlDetail}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10"/></StackPanel>
                            </Border>
                            <Button Content="Elimina riga" Command="{Binding DeleteLineCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                            <Button Content="Duplica riga" Command="{Binding DuplicateLineCommand}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                            <Button Content="＋  Aggiungi riga" Command="{Binding AddLineCommand}" Style="{StaticResource PrimaryButtonStyle}"/>
                        </StackPanel>
                    </Grid>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLine}"
                              AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                              AlternationCount="2" MinHeight="170" MaxHeight="310"
                              HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                              FrozenColumnCount="2" SelectionUnit="FullRow">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="N° pacchi" Width="0.9*" MinWidth="88" Binding="{Binding PackageCountInput, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" ElementStyle="{StaticResource GridDisplayTextStyle}" EditingElementStyle="{StaticResource GridInputStyle}"/>
                            <DataGridTextColumn Header="Pezzi / pacco" Width="1.0*" MinWidth="98" Binding="{Binding PiecesPerPackageInput, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" ElementStyle="{StaticResource GridDisplayTextStyle}" EditingElementStyle="{StaticResource GridInputStyle}"/>
                            <DataGridTextColumn Header="Spessore (mm)" Width="1.15*" MinWidth="112" Binding="{Binding IncomingThickness, UpdateSourceTrigger=PropertyChanged, StringFormat=N2, ValidatesOnDataErrors=True}" ElementStyle="{StaticResource GridDisplayTextStyle}" EditingElementStyle="{StaticResource GridInputStyle}"/>
                            <DataGridTextColumn Header="Larghezza (mm)" Width="1.15*" MinWidth="112" Binding="{Binding IncomingWidth, UpdateSourceTrigger=PropertyChanged, StringFormat=N2, ValidatesOnDataErrors=True}" ElementStyle="{StaticResource GridDisplayTextStyle}" EditingElementStyle="{StaticResource GridInputStyle}"/>
                            <DataGridTextColumn Header="Dopo prepiall. (mm)" Width="1.2*" MinWidth="128" IsReadOnly="True" Binding="{Binding WidthAfterPlaning, StringFormat=N2}" ElementStyle="{StaticResource GridDisplayTextStyle}"/>
                            <DataGridTextColumn Header="Lunghezza (mm)" Width="1.2*" MinWidth="116" Binding="{Binding IncomingLength, UpdateSourceTrigger=PropertyChanged, StringFormat=N2, ValidatesOnDataErrors=True}" ElementStyle="{StaticResource GridDisplayTextStyle}" EditingElementStyle="{StaticResource GridInputStyle}"/>
                            <DataGridComboBoxColumn Header="Qualità" Width="0.9*" MinWidth="92"
                                                    ItemsSource="{x:Static models:GoodsReceiptLine.AllowedQualities}"
                                                    SelectedItemBinding="{Binding Quality, UpdateSourceTrigger=PropertyChanged}">
                                <DataGridComboBoxColumn.ElementStyle>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                        <Setter Property="FontSize" Value="15"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridComboBoxColumn.ElementStyle>
                                <DataGridComboBoxColumn.EditingElementStyle>
                                    <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                                        <Setter Property="FontSize" Value="15"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridComboBoxColumn.EditingElementStyle>
                            </DataGridComboBoxColumn>
                            <DataGridTextColumn Header="MC fisici" Width="1.0*" MinWidth="98" IsReadOnly="True" Binding="{Binding PhysicalIncomingCubicMeters, StringFormat=N2}" ElementStyle="{StaticResource GridDisplayTextStyle}"/>
                            <DataGridTextColumn Header="MC utili teorici" Width="1.1*" MinWidth="120" IsReadOnly="True" Binding="{Binding TheoreticalUsefulCubicMeters, StringFormat=N2}" ElementStyle="{StaticResource GridDisplayTextStyle}"/>
                            <DataGridTextColumn Header="Valore €" Width="1.0*" MinWidth="98" IsReadOnly="True" Binding="{Binding LineValue, StringFormat=N2}" ElementStyle="{StaticResource GridDisplayTextStyle}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <Expander Grid.Row="2" Margin="14,8" Padding="10,7"
                              Background="#101820" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                              Foreground="{DynamicResource PrimaryTextBrush}">
                        <Expander.Header>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="Dettagli calcolo della riga selezionata" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <TextBlock Text="  •  espandi o riduci" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="10.5" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Expander.Header>
                        <Border Background="{DynamicResource SurfaceRaisedBrush}" CornerRadius="10" Padding="14" Margin="0,8,0,0"><UniformGrid Columns="4">
                            <StackPanel Margin="5"><TextBlock Text="Spessore ingresso" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.IncomingThickness, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Spessore convenzionale" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.ConventionalThickness, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Spessore utile finale" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.UsefulProductionThickness, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Larghezza ingresso" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.IncomingWidth, StringFormat={}{0:N2} mm}"/></StackPanel>
                            <StackPanel Margin="5"><TextBlock Text="Prepiallatura applicata" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.PlaningReduction, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Riduzione standard" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.StandardWidthReduction, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Larghezza finale" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.FinalWidth, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Lunghezza ingresso" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.IncomingLength, StringFormat={}{0:N2} mm}"/></StackPanel>
                            <StackPanel Margin="5"><TextBlock Text="Riduzione Finger Joint" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.FingerJointLengthReduction, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Lunghezza finale" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.FinalLength, StringFormat={}{0:N2} mm}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="MC fisici" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.PhysicalIncomingCubicMeters, StringFormat=N2}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="MC utili teorici" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.TheoreticalUsefulCubicMeters, StringFormat=N2}"/></StackPanel>
                            <StackPanel Margin="5"><TextBlock Text="Perdita lavorazione" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.ProcessingLossCubicMeters, StringFormat=N2}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Perdita lavorazione %" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.ProcessingLossPercentage, StringFormat={}{0:N2}%}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Prezzo applicato" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.PrezzoApplicato, StringFormat={}{0:N2} €/m³}"/></StackPanel><StackPanel Margin="5"><TextBlock Text="Valore" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding SelectedLine.LineValue, StringFormat={}{0:N2} €}"/></StackPanel>
                        </UniformGrid></Border>
                    </Expander>
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="RIEPILOGO CARICO" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,13"/>
                    <UniformGrid Rows="1" HorizontalAlignment="Stretch">
                        <UniformGrid.Resources>
                            <Style TargetType="StackPanel">
                                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                            </Style>
                            <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="TextAlignment" Value="Center"/>
                            </Style>
                        </UniformGrid.Resources>
                        <StackPanel><TextBlock Text="Totale pacchi" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalPackages}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource PrimaryTextBrush}"/></StackPanel>
                        <StackPanel><TextBlock Text="Totale pezzi" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalPieces}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource PrimaryTextBrush}"/></StackPanel>
                        <StackPanel><TextBlock Text="MC fisici entrati" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalArrivedCubicMeters, StringFormat=N2}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource PrimaryTextBrush}"/></StackPanel>
                        <StackPanel><TextBlock Text="MC utili teorici" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalTheoreticalCubicMeters, StringFormat=N2}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource PrimaryTextBrush}"/></StackPanel>
                        <StackPanel><TextBlock Text="Perdita lavorazione MC" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalProcessingLossCubicMeters, StringFormat=N2}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource PrimaryTextBrush}"/></StackPanel>
                        <StackPanel><TextBlock Text="Perdita lavorazione %" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalProcessingLossPercentage, StringFormat={}{0:N2}%}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource PrimaryTextBrush}"/></StackPanel>
                        <StackPanel><TextBlock Text="Valore totale €" Style="{StaticResource FieldLabelStyle}"/><TextBlock Text="{Binding TotalValue, StringFormat=N2}" Style="{StaticResource SummaryValueStyle}" Foreground="{DynamicResource AccentBrush}"/></StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <Grid Grid.Row="3" Margin="2,0">
                <TextBlock Text="{Binding ValidationMessage, Mode=OneWay}" Foreground="{DynamicResource DangerBrush}" FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" VerticalAlignment="Center" MaxWidth="820" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Annulla" Click="Cancel_Click" IsEnabled="{Binding IsPrimaryActionEnabled}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,8,0"/>
                    <Button Content="&#xE749;  REGISTRA E STAMPA ETICHETTE" Click="RegisterAndPrint_Click" IsEnabled="{Binding IsPrimaryActionEnabled}" Style="{StaticResource PrimaryButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
